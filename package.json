{
  "private": true,
  "scripts": {
    "build": "docker build -t geospeckle .",
    "dev": "npm run build && docker run -it --name geospeckle -p 5000:80 --rm geospeckle"
  },
  "name": "geospeckle"
}
